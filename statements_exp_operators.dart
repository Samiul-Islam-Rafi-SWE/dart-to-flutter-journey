void main() {
  int a = 10;
  int b = 3;
  var result = a ~/ b;
  print(result);

  // type test operator

  String name = 'samiul';
  var answer = name is String;
  print(answer);

  // ternary operator
  String colour = 'red';
  var ans = colour == 'red' ? 'colour is red' : 'unknone';
  print(ans);

  String value = 'same';
  var total = value == 'same' ? 'reaily same this' : 'not that same';
  print(total);

  // null vlaue


}
